rsvm.browser.MutationObserver = {
    memory: {
    },
    getter: {
    },
    setter: {
    },
    actor: {
        disconnect() {
            if (rsvm.debugconfig.ifdebugger) {
                debugger;
            }
        },
        observe(a) {
            if (rsvm.debugconfig.ifdebugger) {
                debugger;
            }
        },
        takeRecords() {
            if (rsvm.debugconfig.ifdebugger) {
                debugger;
            }
        },

    },
    subObject: []
};